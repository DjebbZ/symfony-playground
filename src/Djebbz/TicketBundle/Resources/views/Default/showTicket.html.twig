{% extends '::base.html.twig' %}

{% block metaTitle %}{{ ticket.title }}{% endblock %}

{% block title %}{{ ticket.title }}{% endblock %}

{% block content %}
    <div class="row">
        <div class="columns small-10 small-centered">
            <h3 class="subheader">{{ ticket.createdAt }}</h3>
            <p>{{ ticket.description }}</p>
        </div>
    </div>
{% endblock %}
